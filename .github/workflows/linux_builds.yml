name: 🐧 Linux Builds
on:
  workflow_call:

# Global Settings
env:
  SCONSFLAGS: verbose=yes warnings=extra werror=yes

jobs:
  linux:
    runs-on: ubuntu-22.04
    name: Linux Build ${{ matrix.target }} ${{ matrix.arch }}
    strategy:
      fail-fast: false
      matrix:
        target: [template_release]
        arch: [x86_64]
        shlibsuffix: [so]

    steps:
      - uses: actions/checkout@v7
        with:
          submodules: true

      - name: Build ${{ matrix.target }} ${{ matrix.arch }}
        uses: ./.github/actions/build
        with:
          sconsflags: ${{ env.SCONSFLAGS }}
          arch: ${{ matrix.arch }}
          platform: linux
          target: ${{ matrix.target }}
          build_profile: build_profile.json

      - name: Upload ${{ matrix.target }} ${{ matrix.arch }}
        uses: ./.github/actions/upload
        with:
          platform: linux
          artifact_file_suffix: linux.${{ matrix.arch }}.${{ matrix.shlibsuffix }}

